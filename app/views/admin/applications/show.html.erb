<h1>Application <%= @application.id %></h1>

<% @application.application_pets.each do |ap| %>
  <div id="app_pet-<%= ap.id %>">
    <% pet = ap.pet %>
    <%= link_to pet.name, pet_path(pet) %><br>
    <% if ap.status == 'Pending' %>
      <%= button_to 'Approve', application_pet_path(ap.pet), method: :patch, params: { application_id: @application.id, status: 'Approve' } %><br>
      <%= button_to "Reject", application_pet_path(ap.pet), method: :patch, params: { application_id: @application.id, status: 'Approve' } %><br>
    <% elsif ap.status == 'Approved' %>
      <%= "#{pet.name} is approved for this application" %>
    <% else %>
      <%= "#{pet.name} has been rejected for this application" %>
    <% end %>
  </div>
<% end %>
